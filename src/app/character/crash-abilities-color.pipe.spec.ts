import { CrashAbilitiesColorPipe } from './crash-abilities-color.pipe';

describe('CrashAbilitiesColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CrashAbilitiesColorPipe();
    expect(pipe).toBeTruthy();
  });
});
